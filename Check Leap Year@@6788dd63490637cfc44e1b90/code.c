#include <stdio.h>

char* welcome() {
    return "Welcome to Code Radar!";
}

int main() {
    int yr;//printf("%s", welcome());
    scanf("%d",&a);
    if ((yr % 4 == 0) && (yr % 100 !== 0) || (yr % 400 == 0)){
        printf("Leap Year");
    }
    else{
        printf("Not a Leap Year");
    }
    return 0;
}